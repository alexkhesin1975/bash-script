# Shell substitutions
# Command substitution example

today_date=`date` #if we want ti insert output of some command to interprettor the command shoud be between apostrofs [ ` == on the ~ button ] [ `date` ]

echo $today_date " seems to be a lovely day!"
